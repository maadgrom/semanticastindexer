# Configuration (`indexer.yaml`)

Controls what gets embedded and pushed. See the shipped `indexer.yaml` for the full
annotated default.

```yaml
backend: qdrant      # qdrant | duckdb
embedder: ort        # duckdb only: ort | ollama
collection: source_code
# When using embedder: ort (the default for local DuckDB), the built-in default is now
# the code-specialized Jina model below. For Qdrant you usually want e5-small.
model: intfloat/multilingual-e5-small
vector_dim: 384      # MUST match (jina code model = 768, e5-small = 384, mxbai=1024)

duckdb:              # backend: duckdb settings
  path: .index/code.duckdb
  # model_repo: jinaai/jina-embeddings-v2-base-code   # default when embedder=ort + Jina model
  # model_repo: Xenova/multilingual-e5-small          # classic e5 for ort
  # model_cache: .model_cache                  # ort offline cache dir

ollama:              # embedder: ollama settings
  # url: http://localhost:11434
  # model: nomic-embed-text                    # REQUIRED for embedder: ollama

exclude_dirs:        # directory names pruned during the walk
  - __tests__
  - node_modules     # (these SKIP_DIRS are always pruned in code too)
  # ...

include: []          # allow-list: when non-empty, ONLY matching files are considered
                     #   include:
                     #     - "src/services/**"
                     #     - "src/hooks/**"

exclude:             # glob patterns ('**' spans directories) matched on each file path
  - "**/*.test.ts"
  - "**/*.spec.tsx"
  - "**/*.d.ts"
  - "**/components/ui/**"   # shadcn primitives
  - "**/*.pb.go"           # Go autogenerated
  # ...

skip_generated_marker: true   # skip files whose head has @generated / "DO NOT EDIT" / etc.
strip_comments: true          # remove // and /* */ before embedding (code only)
```

Qdrant credentials are read **only** from `QDRANT_URL` / `QDRANT_API_KEY` — never put them
in YAML. CLI flags take precedence over `indexer.yaml`.

## Opt-out markers

Add language-agnostic marker comments in your source to control indexing per function/chunk:

| Marker | Behavior |
| ------ | --------- |
| `sai-noindexing` | Skip the chunk **entirely** from indexing — never embedded or stored, so it won't appear in search, `sai_find_similar`, or `sai_find_duplicates`. |
| `sai-noduplicate` | Include in search/`sai_find_similar`, but exclude from near-duplicate clustering (the `duplicates` command / `sai_find_duplicates` MCP tool). |

**Example:**

```typescript
// sai-noindexing
function internalHelper() {
  // This function is never indexed
}

// sai-noduplicate
function intentionallySimilar() {
  // Indexed and searchable, but won't be grouped in duplicates
}
```

Detection is a **case-insensitive substring match** on the raw source (before comment stripping), so it works with any comment syntax: `// sai-noindexing`, `# sai-noduplicate`, `-- sai-noindexing`, `<!-- sai-noduplicate -->`, etc. A string literal containing the marker text would also trigger it — avoid embedding the literal strings in code unless you intend the opt-out.

**Granularity:** With the AST chunker (TS/TSX/Rust/Go), the marker applies to a single function. With the `lines` chunker, the marker applies at window granularity (~60-line chunks with 8-line overlap) — a marker near a window boundary may only drop the window(s) containing it.

**Config toggles** (both default to `true`):

```yaml
honor_noindex_marker: true        # respect sai-noindexing comments
honor_noduplicate_marker: true    # respect sai-noduplicate comments
```

## Selection order

For each file under `--root` (after dir pruning + extension filter):

1. **`include`** (allow-list) — if non-empty, the file must match one include glob, else skipped.
2. **`exclude`** globs — if matched, skipped (exclude always wins over include).
3. **`skip_generated_marker`**, then **`strip_comments`** on the surviving content.

## What's filtered out

1. **Always-pruned dirs** (in code, regardless of config): `node_modules`, `.git`, `dist`,
   `build`, `target`, `.next`, `coverage`, `.turbo`.
2. **`exclude_dirs`** — extra directory names to prune (e.g. `__tests__`).
3. **`exclude`** globs — tests, `*.d.ts`, shadcn `components/ui/**`, autogenerated
   (`*.gen.ts`, `*.pb.go`, `mock_*.go`, …).
4. **`skip_generated_marker`** — content scan of the first ~600 bytes for autogenerated
   markers (`@generated`, Go's `// Code generated … DO NOT EDIT.`, etc.), catching
   generated files that don't follow a naming convention.
5. **`strip_comments`** — for C-family files, removes `//` and `/* */` comments so only
   code reaches the backend. String/template literals are preserved and **line numbers stay
   accurate** (payload `start_line`/`end_line` still point at the real file).

## Limitations

- Regex literals aren't tracked by the comment stripper, so a `//` *inside* a regex
  literal would be treated as a comment. Extremely rare in practice.
- Comment stripping covers C-family languages only (`ts, tsx, js, jsx, go, rs, java, …`);
  other languages are indexed verbatim.
- The **ast** chunker covers **TypeScript/TSX, Rust, and Go**; everything else falls back to
  the line chunker.
